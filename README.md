# gocalc

#### Terdapat dua versi pada module gocalc dan perbedaan besar terhadap versi tersebut yaitu perbedaan nama fungsi utama untuk membuat instansi dan memulai operasi matematika berantai.
- `v1` -> `NewCalc()`
- `v2` -> `New()`